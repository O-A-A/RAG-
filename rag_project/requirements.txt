# ============================================================================
# RAG 检索策略对比实验 — 项目依赖
# ============================================================================
# 安装方式: pip install -r requirements.txt
# 建议使用 Python 3.10+ 并创建虚拟环境
# ============================================================================

# ------------------------------ LangChain 生态 ------------------------------
langchain>=0.3.0
langchain-community>=0.3.0
langchain-huggingface>=0.1.0
langchain-core>=0.3.0
langchain-text-splitters>=0.3.0

# ------------------------------ 向量检索 ------------------------------------
faiss-cpu>=1.8.0              # FAISS 向量相似度搜索 (CPU 版本)
# faiss-gpu>=1.7.2            # FAISS GPU 版本 (如有 NVIDIA GPU，替换上面 faiss-cpu)

# ------------------------------ 深度学习框架 --------------------------------
torch>=2.0.0
transformers>=4.40.0
sentence-transformers>=3.0.0   # BGE 等句子嵌入模型依赖
accelerate>=0.20.0             # 模型分布式加载与推理加速
bitsandbytes>=0.41.0           # 4-bit 量化加载 (节省 GPU 显存)

# ------------------------------ 稀疏检索 ------------------------------------
rank-bm25>=0.2.2               # BM25 算法实现 (轻量、学术引用广泛)
# ------------------------------ 中文处理 ------------------------------------
jieba>=0.42.1                  # 中文分词 (BM25 检索必需)

# ------------------------------ 工具库 --------------------------------------
numpy>=1.24.0
scipy>=1.10.0                  # BM25 稀疏矩阵计算
tqdm>=4.65.0                   # 进度条显示
PyYAML>=6.0                    # YAML 配置文件解析 (可选，配置扩展用)

# ------------------------------ 可选依赖 ------------------------------------
# vllm>=0.5.0                  # 高性能 LLM 推理引擎 (替代 HuggingFace Pipeline)
# xformers>=0.0.20             # 高效注意力机制 (加速 Transformer)
